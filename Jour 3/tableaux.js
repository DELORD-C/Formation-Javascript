let tableau = ['Mercedes', 3, false, 'dernierElem'];
let tableauNum = [98, 156, 1, 3, 7, 26];

// tableau.sort(); // sort permet de trier un tableau par ordre alphabetique

// tableau.splice(0, 2); 
//Splice(POSITION, NOMBRE A SUPPRIMER, NOUVEUX ELEMENTS) sert à supprimer et/ou ajouter des élements dans un tableau

// tableau.push('nouvelElem4'); // push sert a rajouter un élément à la fin d'un tableau

// let miniTableau = tableau.slice(1, 3); //slice sert à créer un tableau basé sur un interval d'un autre tableau
// début inclu // fin exclue
// console.log(miniTableau);

// console.log(tableau.pop()); //pop sert à retirer le dernier élement d'un tableau et d'en récupérer la valeur
// console.log(tableau);

// console.log(tableau.shift()); //shift sert à retirer le premier élement d'un tableau et d'en récupérer la valeur
// console.log(tableau);

// console.log(tableau.reverse()); //reverse sert à inverser un tableau

// let resultat = tableauNum.find(element => element > 50);
// find récupère le premier élement respectant la condition qu'on lui donne en paramètre

// let resultat = tableauNum.findIndex(element => element > 50);
//find récupère l'index du premier élement respectant la condition qu'on lui donne en paramètre
// console.log(resultat);

